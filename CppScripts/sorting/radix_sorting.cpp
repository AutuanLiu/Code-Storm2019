// 基数排序

#include "sorting.h"

void radix_sort(vector<int>& nums)
{

}

int main()
{
	vector<int> nums = { 54, 38, 96, 23, 15, 72, 60, 45, 83, -1, -23 };
	print_vector(nums);
	radix_sort(nums);
	print_vector(nums);
	return 0;
}
